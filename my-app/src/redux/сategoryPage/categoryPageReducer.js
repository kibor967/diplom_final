import { dataCategoryPage } from '../../dataPages/dataCategoryPage/dataCategoryPage.js';

const initialState = dataCategoryPage;

export const categoryPageReducer = (state = initialState, action) => {
	return state;
};
