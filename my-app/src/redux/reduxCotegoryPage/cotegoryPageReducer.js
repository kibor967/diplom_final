import { dataCotegoryPage } from '../../dataPages/dataCotegoryPage/dataCotegoryPage.js';

const initialState = dataCotegoryPage;

export const cotegoryPageReducer = (state = initialState, action) => {
    return state
};